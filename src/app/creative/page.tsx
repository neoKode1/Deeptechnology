import { redirect } from 'next/navigation';

/**
 * Creative/Media page — temporarily deactivated.
 * Re-enable by restoring the original page content from git history.
 */
export default function CreativePage() {
  redirect('/');
}
